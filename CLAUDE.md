# CLAUDE.md — Quy ước làm việc cho dự án WMS 26house

## Ngôn ngữ
- **Tất cả file `.md`** (tài liệu, kế hoạch, task list) viết bằng **tiếng Việt**.
- **Mọi file khác** (SQL, `.apx`, PL/SQL, JavaScript, JSON...) viết bằng **tiếng Anh** (tên bảng/cột, comment code, message lỗi, label UI trong APEX).

## Tài liệu tham chiếu, đọc theo thứ tự
1. `26house-new-system.docx` — tài liệu yêu cầu nghiệp vụ gốc (tiếng Việt, từ SIEA/Goldmann). Nguồn sự thật duy nhất cho phạm vi dự án — mọi thay đổi yêu cầu phải đối chiếu lại file này trước.
2. `PROJECT-PLAN.md` — kế hoạch tổng: tóm tắt yêu cầu (FR-01..FR-10), mô hình dữ liệu, mapping module→APEXlang, rào chắn tuân thủ, cách test.
3. `TASKS.md` — trạng thái triển khai theo từng FR + task cụ thể cần làm tiếp, cập nhật liên tục.
4. `README.md` — hướng dẫn cài đặt môi trường, thứ tự chạy DDL/package/seed, quy trình import `.apx` vào APEX, cách truy cập app. Đọc file này khi cần **làm** (chạy lệnh) thay vì chỉ đọc bối cảnh.

## Bối cảnh kỹ thuật
- App APEX: `26HOUSE`, application_id = **105**, workspace = **TDT203** (workspace_id = 6802490454262707).
- DB: Oracle 26ai Free chạy trong Docker container `apex-db` (cổng 1521, service `FREEPDB1`). Schema parse của app = **ADMIN**.
- Kết nối SQLcl đã lưu sẵn tên `admin_freepdb1` (dùng cho mọi lệnh `apexctl.mjs runtime validate/roundtrip --db-connection-name admin_freepdb1`).
- Bộ công cụ **APEXlang** nằm ở `apexlang/` (bên trong chính repo này) — dùng `.apx` DSL, compile qua `node apexlang/tools/apexctl.mjs`.

## ⚠️ Lưu ý quan trọng: schema DB đã có sẵn 2 bộ bảng song song
Schema `ADMIN` có sẵn **một bộ bảng WMS khác** (kiểu Odoo, có trước dự án này): `PRODUCTS`, `WAREHOUSES`, `STOCK_LOCATIONS`, `STOCK_LOTS`, `STOCK_QUANTS`, `STOCK_QUANT_LOTS`, `STOCK_PICKINGS`, `STOCK_MOVES`, `QUALITY_INSPECTIONS`, `QC_CRITERIA`, `WMS_ERRORS`, `WMS_KPI_METRICS`, `WMS_STOCKTAKES*`, cùng package `INV_PKG`, `ORDER_PKG`, `WMS_KPI_PKG`. Theo quyết định của người dùng, dự án này xây **schema mới, độc lập, không tái sử dụng** bộ bảng cũ (tên bảng số ít: `ITEM`, `LOCATION`, `LOT`, `WAREHOUSE`, `SUPPLIER`, `INVENTORY_BALANCE`...).

Hệ quả cần nhớ khi tạo bảng/constraint mới:
- **Tên constraint là duy nhất theo schema trong Oracle** (không theo bảng) — luôn kiểm tra trùng tên với bộ bảng cũ trước khi đặt tên constraint. Ví dụ đã gặp: `LOT_PK` bị trùng với `STOCK_LOTS.LOT_PK` → phải đổi prefix bảng `LOT` mới thành `LOTM_*`.
- Trước khi thiết kế bảng mới, `describe` nhanh các bảng cũ cùng chủ đề để chắc chắn không trùng tên bảng/index.

## Lưu ý cú pháp APEXlang đã gặp lỗi thực tế (tránh lặp lại)
- `template: @Region` **không tồn tại** — dùng `template: @/standard` cho region Interactive Grid thông thường.
- Column trong Interactive Grid **không được phép** có block `comments {}` (dù grammar chung có liệt kê) — validator sẽ báo `DSL_RULE_BLOCK`. Bỏ block này, ghi chú business rule (vd danh sách giá trị hợp lệ) ở nơi khác (README/comment DB) thay vì trong `.apx`.
- Interactive Grid nguồn `table-backed` (`source.tableName`) cần offline schema metadata mà môi trường này không có sẵn → luôn dùng **query-backed** (`source.type: sqlQuery`) thay thế, order by ngay trong SQL.
- Column trong Interactive Grid **không được phép** có block `appearance {}` (kể cả `formatMask` trên cột `datePicker`) — dù template mẫu `interactive-grid.example.md` có dùng, live validator của bản build hiện tại (26.1.0+3102) từ chối với `DSL_RULE_BLOCK`. Bỏ hẳn block này, dùng format mặc định của APEX.
- Mọi trang không phải modal **bắt buộc** có region `type: breadcrumb` (`source.breadcrumb: @breadcrumb`) — kể cả trang Home.
- **[ĐÃ SỬA — xem bản dưới] Column trong Interactive Grid THỰC RA CÓ dùng được block `lov {}`.** Lần đầu gặp `DSL_RULE_BLOCK ... does not allow block 'lov'` từ `node apexctl.mjs apexlang validate` (local lint, dựa theo `apexlang/assets/component-attributes.json` — JSON này liệt kê `region.interactiveGrid.column.allowedBlocks` KHÔNG có `lov`), nên từng kết luận nhầm là không làm được và tách app thành 8 cặp trang Report(IR)+Form(modal) để né. Sau đó **test trực tiếp qua `runtime roundtrip` (live SQLcl `apex validate`/`apex import`) thì `lov{}` trên cột Interactive Grid PASS và IMPORT THÀNH CÔNG** (`local_check_status: fail` nhưng `live_check_status: pass`, `import_status: pass`, 0 problems) — tức là **local lint ở bản build này sai (false positive)** cho riêng rule này, không phản ánh đúng khả năng của live APEX engine. Đã revert lại 8 trang Report+Form về đúng 1 trang **Interactive Grid** duy nhất (giống `Customers`/`UOM`), cột many2one dùng thẳng `column X (type: selectList, lov { type: sharedComponent, lov: @<shared-lov> })` ngay trong grid — **không cần** tách Form riêng nữa. Kết luận chung: **khi local lint và live SQLcl bất đồng, luôn tin live SQLcl** (đúng theo policy đã ghi ở mục "Quy trình validate" bên dưới) — đừng dừng lại ở lỗi local lint mà chưa thử `runtime roundtrip` thật.
- Cú pháp `column X (type: selectList, lov { type: sharedComponent | sqlQuery | staticValues, ... })` áp dụng được cho **cả Interactive Grid lẫn cột nào cần LOV**: `type: sharedComponent` + `lov: @<alias>` để dùng LOV dùng chung khai báo ở `shared-components/lovs.apx` (mục `lov <alias> (name, source{type: sqlQuery, sqlQuery}, columnMapping{return, display})`); hoặc `type: staticValues` + `staticValues: STATIC:Display1;Value1,Display2;Value2` cho danh sách cố định (vd enum theo CHECK constraint) — không cần validate qua trang Form riêng.
- **Interactive Grid Master-Detail (1 trang header + nhiều dòng con) làm được thật**, dù local lint cũng báo sai (`DSL_RULE_BLOCK ... column ... does not allow block 'masterDetail'`) y hệt kiểu lỗi `lov` ở trên — đã test qua `runtime roundtrip` live và import sạch (page Inbound Receipts, gộp header `INBOUND_RECEIPTS_GRID` + dòng `INBOUND_RECEIPT_LINES_GRID` trên cùng 1 trang). Cú pháp: region con (detail) thêm `masterDetail { masterRegion: @<region-alias-cua-header> }` (ngang hàng với `edit{}`/`pagination{}`, chỉ áp dụng khi `type: interactiveGrid`); cột FK trong detail trỏ về PK của header thêm `masterDetail { masterColumn: @<column-alias-cot-PK-o-header> }`, nên đặt `type: hidden` cho cột FK này (giá trị tự động gán từ dòng header đang chọn khi thêm dòng mới, người dùng không cần nhập) và **đặt static id khác tên với cột PK bên header** (vd header có `column RECEIPT_ID`, detail nên đặt `column LINE_RECEIPT_ID` dù `source.databaseColumn` vẫn là `RECEIPT_ID`) để tránh trùng static id giữa 2 region trên cùng 1 trang. SQL của detail region **không cần** tự thêm `where receipt_id = ...` — APEX tự lọc theo dòng header đang được chọn dựa trên khai báo `masterDetail` này.
- `type: checkbox` cũng dùng tốt trên cột Interactive Grid (`column X (type: checkbox, source{databaseColumn, dataType: varchar2})`), APEX tự hiểu Checked='Y'/Unchecked='N' khớp dữ liệu Y/N sẵn có — dùng cho mọi cột boolean, kể cả ở các bảng có cột many2one khác (không cần tách sang selectList Yes/No hay Form riêng như từng làm).
- `template: @Region` **không tồn tại** — dùng `template: @/standard` cho region Interactive Grid thông thường.
- Column trong Interactive Grid **không được phép** có block `comments {}` (dù grammar chung có liệt kê) — validator sẽ báo `DSL_RULE_BLOCK`. Bỏ block này, ghi chú business rule (vd danh sách giá trị hợp lệ) ở nơi khác (README/comment DB) thay vì trong `.apx`.
- Interactive Grid nguồn `table-backed` (`source.tableName`) cần offline schema metadata mà môi trường này không có sẵn → luôn dùng **query-backed** (`source.type: sqlQuery`) thay thế, order by ngay trong SQL.
- Column trong Interactive Grid **không được phép** có block `appearance {}` (kể cả `formatMask` trên cột `datePicker`) — dù template mẫu `interactive-grid.example.md` có dùng, live validator của bản build hiện tại (26.1.0+3102) từ chối với `DSL_RULE_BLOCK`. Bỏ hẳn block này, dùng format mặc định của APEX. (Khác với `lov`/`checkbox` ở trên — `appearance`/`comments` đã test và **live compiler cũng từ chối thật**, không phải false positive của local lint.)
- Mọi trang không phải modal **bắt buộc** có region `type: breadcrumb` (`source.breadcrumb: @breadcrumb`) — kể cả trang Home.
- Shared LOV dùng chung đặt ở `shared-components/lovs.apx` (`item-active`, `uom-active`, `warehouse-active`, `supplier-active`, `customer-active`, `location-active`, `lot-any`, `inbound-receipt-any`, `outbound-order-any`) — mọi LOV cho bảng có cột `is_active` đều lọc `where is_active = 'Y'` (theo yêu cầu người dùng: many2one chỉ được chọn bản ghi đang active). Cột `is_active`/boolean khác mặc định `Y` (true) ngay ở DB (`default 'Y'`), không cần default riêng ở `.apx`.

## Quy trình validate + import (đúc rút từ thực tế chạy)
1. Sửa `.apx` xong, chạy `node apexlang/tools/apexctl.mjs apexlang validate --app-path <repo_root>` để bắt lỗi cú pháp cơ bản trước.
2. **Không** chạy `runtime validate`/`runtime roundtrip` thẳng vào `--app-path` = root repo — do repo để chung toolkit `apexlang/` (có chứa `apexlang/templates/base-app-structure/scaffold-example/` là một app mẫu đầy đủ khác) nên tool đôi khi quét nhầm sang app mẫu đó thay vì app thật, gây báo lỗi sai be bét.
   - Cách xử lý: copy đúng các thành phần app thật (`application.apx`, `page-groups.apx`, `pages/`, `shared-components/`, `deployments/`, `.apex/`) sang một thư mục sạch trong scratchpad, rồi `runtime validate`/`runtime roundtrip` trỏ vào thư mục sạch đó.
   - Sau khi import xong, `diff -rq` thư mục sạch với thư mục gốc trong repo để đảm bảo không lệch (vì import không sửa ngược file nguồn).
3. `compiler-truth audit` offline **không chạy được** trong môi trường này (thiếu Oracle APEXlang runtime jar / VS Code extension) — bỏ qua, coi live validate qua SQLcl là bước xác thực có thẩm quyền cuối cùng (đúng theo chính sách của workflow: local check chỉ là advisory khi live validate đã pass).
4. Lệnh import thật: `runtime roundtrip --import-intent validate-and-import --target-resolution-mode update-existing --skip-runtime-verification --execution-mode path --workspaceid 6802490454262707` — **bắt buộc phải có `--workspaceid` tường minh**, nếu không sẽ bị chặn với lỗi `lookup_scope_workspace_missing` dù `--db-connection-name` đã đúng.
5. App càng nhiều trang thì `runtime roundtrip` càng lâu — timeout 240s từng đủ cho ~10 trang nhưng bị `live_validate_timeout` khi app lên 14+ trang. Dùng `timeout 480` (hoặc hơn) cho các lần import sau khi app đã lớn.

## Trước khi tạo bảng mới: luôn kiểm tra trùng tên constraint
Chạy `select constraint_name from user_constraints order by 1;` và rà bằng mắt trước khi đặt tên constraint cho bảng mới, thay vì đợi lỗi `ORA-02264` rồi sửa. Các prefix đã dùng (không được đụng lại cho bảng khác ngoài bảng đã liệt kê):
`SUPPLIER_`, `WAREHOUSE_`, `LOCATION_`, `ITEM_`, `LOTM_` (bảng `LOT`, tránh `LOT_` đã bị `STOCK_LOTS` chiếm), `INVBAL_`/`INVENTORY_BALANCE_`, `INVTXN_`, `INBRCPT_`/`INBLINE_`, `CUSTOMER_` (tránh `CUST_` đã bị `CUSTOMERS` cũ chiếm), `OBORDER_`/`OBLINE_` (tránh `ORD_`/`ORDI_` đã bị `ORDERS`/`ORDER_ITEMS` cũ chiếm), `PKWAVE_`/`PKTASK_` (tránh `PICK_` đã bị `STOCK_PICKINGS` chiếm), `SHPKG_`/`SHPKGL_`/`SHIP_`, `KPIBASE_` (tránh `KPIM_` đã bị `WMS_KPI_METRICS` chiếm).

## Package PL/SQL đã có (db/plsql/)
`PKG_ITEM`, `PKG_LOCATION`, `PKG_LOT`, `PKG_INVENTORY` (điểm post tồn kho duy nhất + `get_location_by_type` dùng chung), `PKG_INBOUND` (receive/putaway), `PKG_OUTBOUND` (order/allocate — có kiểm tra QC status + quality hold), `PKG_PICKING` (confirm_pick — validate scan), `PKG_PACKING` (pack_order), `PKG_SHIPMENT` (ship_order), `PKG_QUALITY` (record_inspection/place_hold/release_hold), `PKG_INTEGRATION_OUT` (publish_order_event), `PKG_INTEGRATION_IN` (stage_payload/apply_order_import). Module sau luôn gọi `PKG_INVENTORY` để đổi tồn kho, không update `INVENTORY_BALANCE` trực tiếp.

## Autonomous transaction: tự deadlock nếu đụng dòng đang bị khóa bởi transaction chính
Gặp lỗi này 2 lần khi viết `PKG_INTEGRATION_IN` (procedure ghi lỗi dùng `pragma autonomous_transaction` để log ra `INTEGRATION_LOG` dù transaction chính rollback). Quy tắc:
- `pragma autonomous_transaction` **chỉ hợp lệ** ở cấp procedure/function độc lập hoặc trong package — **không** hợp lệ trong 1 block `declare...begin...end` lồng bên trong 1 block khác (kể cả exception handler). Lỗi biên dịch: `PLS-00710: Pragma AUTONOMOUS_TRANSACTION cannot be specified here`. Cách sửa: tách thành 1 procedure riêng trong package body (private, không cần khai báo ở spec).
- Nếu transaction chính đang giữ khóa trên đúng dòng mà autonomous transaction cố UPDATE (do `SELECT ... FOR UPDATE`, **hoặc đơn giản là do chính transaction đó vừa `INSERT` dòng này và chưa commit**), autonomous transaction sẽ tự deadlock với chính transaction cha của nó → `ORA-00060: deadlock detected while waiting for resource`, và autonomous transaction bị rollback (không raise lỗi rõ ràng lên caller nếu procedure log lỗi không có exception handler riêng — chỉ âm thầm không ghi được gì). Cách sửa: không giữ `FOR UPDATE` trên dòng đó nếu về sau có thể bị autonomous transaction khác update; và bước "ghi nhận đã nhận" (staging) nên tự commit ngay (autonomous) thay vì để chung transaction với bước "xử lý" (apply) phía sau — đúng bản chất nghiệp vụ tích hợp (staging phải sống sót độc lập với thất bại của bước xử lý).
- Luôn viết test gọi hàm PL/SQL **có gán biến trả về** (`l_x := pkg.func(...)`) — gọi hàm như procedure (`pkg.func(...);` không gán) là lỗi biên dịch `PLS-00221`, gặp nhiều lần khi viết script test nhanh.

## Quy ước cột `is_active` vs `status`
Theo yêu cầu người dùng, mọi bảng master data mới: dùng cột **`is_active char(1)` (Y/N, boolean)** riêng cho việc bật/tắt bản ghi (enable/disable) — **không** gộp chung với `status`. Cột `status varchar2` chỉ dùng cho **trạng thái nghiệp vụ/workflow thật** (vd `nhập`/`RECEIVED`, `xác nhận`/`CONFIRMED`, `đã giao`/`SHIPPED`...) của các bảng chứng từ có vòng đời (`INBOUND_RECEIPT*`, `OUTBOUND_ORDER*`, `PICK_WAVE`, `PICK_TASK`, `SHIP_PACKAGE`, `SHIPMENT`, `INTEGRATION_LOG`, `QUALITY_HOLD`...) — các bảng này giữ nguyên `status`, không đổi. Đã áp dụng: `ITEM.status` (ACTIVE/INACTIVE/DISCONTINUED) và `UOM.status` (ACTIVE/INACTIVE) đổi thành `is_active` (Y/N), khớp với pattern đã có sẵn ở `SUPPLIER/WAREHOUSE/LOCATION/CUSTOMER.is_active`. Constraint đổi tên tương ứng: `ITEM_STATUS_CK`→`ITEM_ACTIVE_CK`, `UOM_STATUS_CK`→`UOM_ACTIVE_CK`.

## Dữ liệu giả định (placeholder) — chỉ dùng khi được người dùng cho phép rõ ràng
Bảng `KPI_BASELINE` hiện có 3 dòng dữ liệu **giả định** (`IS_PLACEHOLDER='Y'`), do người dùng chủ động cho phép "dữ liệu có thể fake" trong lúc dev. Theo đúng cảnh báo trong `26house-new-system.docx`/`WMS_KPI_METRICS` cũ: "baseline PHẢI là số đo thật, nếu không sẽ không chứng minh được mức cải thiện" — **phải thay bằng số liệu thật và set `is_placeholder='N'` trước khi UAT/nộp báo cáo SIEA**. Không tự ý tạo thêm dữ liệu giả định cho các bảng khác nếu chưa được người dùng xác nhận.

## Nguyên tắc tuân thủ tài trợ SIEA (nhắc lại từ PROJECT-PLAN.md)
Không được thiết kế/code bất kỳ tính năng nào thuộc danh sách bị cấm trong `PROJECT-PLAN.md` mục 1.2 (CRM, HR/chấm công, hóa đơn, marketing, phân tích thị trường/tài chính, website, mua sắm phần mềm/phần cứng có sẵn). Nếu yêu cầu mới có dấu hiệu rơi vào các mục này, dừng lại và hỏi người dùng trước khi code.
